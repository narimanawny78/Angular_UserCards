import { EmailVailidateDirective } from './email-validate.directive';

describe('EmailVailidateDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailVailidateDirective();
    expect(directive).toBeTruthy();
  });
});
